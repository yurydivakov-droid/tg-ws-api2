{
  "name": "tg-ws-api2",
  "version": "1.0.0",
  "description": "Cloudflare Worker — WebSocket proxy for Telegram MTProto connections",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.71.0"
  }
}
